<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error - Something Went Wrong</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #ff4e50, #f9d423);
      color: #fff;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .error-container {
      margin-top: 10vh;
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(8px);
      border-radius: 12px;
      padding: 40px;
      text-align: center;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
    }
    .error-code {
      font-size: 5rem;
      font-weight: bold;
    }
    .error-message {
      font-size: 1.3rem;
      margin-bottom: 20px;
    }
    .btn-home {
      font-size: 1.1rem;
      padding: 10px 20px;
      border-radius: 50px;
      background-color: #fff;
      color: #333;
      transition: all 0.3s ease;
      text-decoration: none;
    }
    .btn-home:hover {
      background-color: #333;
      color: #fff;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="col-md-6 offset-md-3 error-container">
      <div class="error-code">
        <%= err.statusCode || 500 %>
      </div>
      <h2 class="error-message"><%= err.message || "Something went wrong!" %></h2>
      <p>Please try again later or return to the homepage.</p>
      <a href="/listings" class="btn btn-home mt-3">Go to Home</a>
    </div>
  </div>
</body>
</html>
